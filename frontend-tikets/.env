# Frontend API Configuration
# En desarrollo con Docker, el frontend (en tu navegador) se conecta al backend
# a través del puerto mapeado en la máquina host.
# En producción, reemplaza esta URL por la URL pública de tu Application Load Balancer (ALB).
VITE_API_BASE_URL=http://localhost:4000/api
